# tools/ 에서 사용 (export_weights_to_bin, preprocess_image_to_bin, gen_test_vectors 등)
numpy>=1.20
torch>=1.9
Pillow>=8.0
# export_weights_to_bin.py --classic (anchor-based YOLOv5n) 사용 시
# ultralytics>=8.0

# 설치 방법 (프로젝트 루트에서)
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
# Windows: .venv\Scripts\pip install -r requirements.txt
# 사용: .venv/bin/python tools/quantize_weights.py ...  또는 .venv/bin/python tools/preprocess_image_to_bin.py ...
